def(orElse, cond (#0, #0, #1)).

def(distinct, j::it::distinct(#0)).

def(lift, 
    _{ new $lifted,
       *.*._{ selector $p, $lifted.property($p): _ }
     }
     . $lifted
).

def(embed, 
    _ $root{ 
       #0.*
       ._{ selector $p,
           $root.property($p) : _ } 
    }    
)
